import 'package:flutter/material.dart';

class Abonelik extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("ABONELİK"),
        centerTitle: true,
        backgroundColor: Colors.lightBlue,
      ),
    );
  }
}
