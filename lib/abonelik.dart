import 'package:flutter/material.dart';

class Abonelik extends StatelessWidget {
  const Abonelik({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("ABONELİK"),
        centerTitle: true,
        backgroundColor: Colors.lightBlue,
      ),
    );
  }
}
